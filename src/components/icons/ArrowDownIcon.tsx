import React from "react";
import ExpandMoreRoundedIcon from "@mui/icons-material/ExpandMoreRounded";

const ArrowDownIcon = () => {
  return <ExpandMoreRoundedIcon fontSize="medium" className="" />;
};

export default ArrowDownIcon;
